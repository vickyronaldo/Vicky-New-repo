Resolved. Cause: Finance shared drive mapping script executed in SYSTEM context after migration from GPO USER-context logon script, causing UNC path access failure during login timing and resulting in missing S: drive assignment. Evidence: Intune ScriptRunner logs show `Map-FinBridgeDrives.ps1` failed with exit code 1 and "Network name cannot be found"; System log shows Workstation service running and Group Policy success (Event 1500), confirming this was not a GP issue. Action: Updated drive-mapping execution to a user-session-compatible method, redeployed to Finance OU devices, and added retry/validation checks. Outcome: shared drive access restored for affected users, with post-fix checks showing successful mapping. User confirmed working.
