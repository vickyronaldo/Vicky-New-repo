Resolved. Cause: Floor 3 DHCP scope still assigned a decommissioned DNS server after overnight migration, which prevented domain controller name resolution and Group Policy processing. Action: Updated DHCP DNS scope to the correct server values, removed old DNS references, renewed affected client DHCP/DNS state, flushed DNS cache, and forced Group Policy refresh; service restored and verified at 10:00. Preventive: Enforce a DHCP-DNS dependency gate before DNS decommission, add subnet synthetic checks for DC lookup and GP processing, and monitor DHCP Option 006 drift. User confirmed working.
